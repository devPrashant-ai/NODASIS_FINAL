# NODASIS Pro Lite v2.9 (MongoDB Edition)

This system runs full NEPSE operator prediction with MBMS models, DRL, and MongoDB.

- Run with `python3 run_scoring_pipeline.py`
- Launch dashboard with `streamlit run streamlit_app.py`
